export enum JustifyContent {
    Start,
    Center,
    SpaceBetween,
    SpaceAround,
    SpaceEvenly,
    End
}

export enum AlignItems {
    Start,
    Center,
    End
}

export enum GridConstraint {
    FixedColumnCount,
    FixedRowCount
}